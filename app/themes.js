import { baseColor } from './baseColor';

export const themes = {
  dark: {
    bgColor: baseColor.primary,
    fontColor: '#ffffff',
  },
  light: {
    bgColor: '#ffffff',
    fontColor: baseColor.primary,
  },
};
